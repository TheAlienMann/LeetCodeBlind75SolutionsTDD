import XCTest
@testable import SerializeAndDeserializeBST

final class SerializeAndDeserializeBSTTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SerializeAndDeserializeBST().text, "Hello, World!")
    }
}
