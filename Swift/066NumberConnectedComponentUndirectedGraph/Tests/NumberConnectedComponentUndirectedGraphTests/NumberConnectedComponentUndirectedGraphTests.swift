import XCTest
@testable import NumberConnectedComponentUndirectedGraph

final class NumberConnectedComponentUndirectedGraphTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NumberConnectedComponentUndirectedGraph().text, "Hello, World!")
    }
}
